dependencies:

  - CURL (sudo apt-get install libcurl4-gnutls-dev)
  - JSONCPP (sudo apt-get install libjsoncpp-dev)

release build: 
	make

debug build:
	make debug

to execute: open index.html in firefox, and execute "run.sh"
